/* eslint no-shadow: 0 */
import test from 'tape';

//eslint-disable-next-line
import middlewares from '../';

test('Include all middlewares', (t) => {
  t.test('Include all middlewares', (t) => {
    t.end();
  });
});
